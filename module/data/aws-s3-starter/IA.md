# IA

Actúa como orquestador según CLAUDE.md.

Antes de empezar: verifica que `.template-guard` NO existe.
Si existe: ejecuta `./template-init.sh` primero.

Luego implementa las features de `feature_list.json` en orden (1 → 12),
siguiendo el workflow de CLAUDE.md.

Comienza ahora con la feature 1.
